spring:
  data:
    mongodb:
      username: "mongo"
      password: "password"
  mail:
    password: "password"
    host: mail.localhost
    port: 587
    username: service@localhost
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enabled: true
            required: true
          ssl.trust: mail.localhost

influx:
  url: "http://localhost:8086"
  token: "password"

management:
  server:
    port: 8061

server:
  port: 8051